#include <stdio.h>
#include "main.h"
/**
 *swap_int - swaps the value of two integer.
 *@a: swaps with b
 *@b: swaps with a
 *Return: Always 0
*/
void swap_int(int *a, int *b)

{
int *a;
int *b;
swap_int(&a, &b);
}
